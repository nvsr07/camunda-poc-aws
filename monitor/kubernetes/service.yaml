apiVersion: v1
kind: Service
metadata:
  name: eep-order-api-kafka-monitor
spec:
  selector:
    app: eep-order-api-kafka-monitor
  type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 80
      targetPort: 8095